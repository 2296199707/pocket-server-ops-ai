# PocketServerOps AI v1.0.5-beta.14

- 优化可恢复远程命令：`terminal.exec` 将进程启动和首次状态探测合并到一次 SSH 命令请求。
- 保留远程进程幂等启动、断线恢复、输出偏移读取、审批和写操作顺序，不自动重放有副作用命令。
- 明确保持每轮根据本地完整历史发送请求，不启用 `previous_response_id`、供应商服务端状态或 WebSocket 增量续接。
- 修复并发只读工具和远程进程输出轮询的固定开销，同时保留写后读顺序。

定向 Agent、SSH 远程进程、Responses 和 Chat Completions 测试通过；未使用真实供应商或真实服务器。

Android 版本：1.0.5-beta.14（versionCode 58）。
