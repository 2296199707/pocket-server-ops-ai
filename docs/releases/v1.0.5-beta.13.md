# PocketServerOps AI v1.0.5-beta.13

- 修复模型刷新只读取普通 `/models` 导致推理强度无法获取的问题。
- 普通模型列表继续保留供应商返回的全部模型；在普通条目没有能力字段时，额外请求带 `client_version` 的 Codex 模型清单。
- 按精确模型 ID 合并 `default_reasoning_level` 和 `supported_reasoning_levels`，能力清单失败不会影响普通模型刷新。
- 按 Sub2API 最新源码更新 Codex 能力清单请求版本，并记录源码与真实接口验证结果。

定向模型刷新、Codex 元数据解析和上下文模型元数据测试通过；未启动重复构建任务。

Android 版本：1.0.5-beta.13（versionCode 57）。
