# HY2 + SSH 流量统计

## 2026-09-19 可选流量模块（本轮进度）

- 按用户要求，新增“设置 → 仪表盘设置 → 流量监控”，全局开关默认关闭，保存到本机设置。
- 关闭时隐藏 HY2 状态、SSH/HY2 流量及合计；仍保留 CPU、内存、磁盘与整机网卡指标。开启后显示目标服务器的配置入口，统计规则仍按服务器手动安装。
- 开关只控制 APP 采集和显示，不向所有服务器自动安装，也不删除已有 nft 表/清零历史累计。需要停用服务器计数时仍通过卡片的“停用统计”。
- 关闭时不发送端口探针、不调用 HY2 API。旧版已安装状态脚本会无条件探测 HY2，因此关闭时直接读取基本指标；无需重新安装即可生效，仍正确显示脚本已安装状态。
- 新状态脚本版本 5，HY2 部分由显式环境开关控制，默认不执行。请求去重区分开关状态，切换前的迟到请求不覆盖当前缓存。
- 主代理负责设置持久化、采集与旧脚本处理；子代理负责设置子页、仪表盘可见性及 UI 测试。
- 已通过 4 项定向测试：旧安装脚本关闭时不执行 HY2 探针、新脚本默认不执行 HY2 检测、服务器缓存独立、控制器关闭/开启/再关闭的采集行为。
- 另 11 项 UI 测试通过，覆盖设置入口、默认关闭、开关持久化、返回后状态文案同步、缓存流量卡随开关隐藏/显示，以及原设置和端口配置卡回归。主代理已审阅 UI 改动。
- 控制器、脚本、UI 及新增测试静态检查通过，git diff --check 通过。
- 未连接或修改真实服务器，未构建 APK、推送或发布。此前手机及服务器图像能力的未提交修改继续保留。

2026-09-20 发布收尾：本功能与图像能力一起打包为 `1.0.7-beta.3+62`，Release 构建及签名校验通过；具体产物、摘要与验证范围见 `docs/releases/v1.0.7-beta.3.md`。

2026-09-13，接续未提交的轻量仪表盘改动。

## 口径与范围

- HY2 官方 /traffic 是代理业务字节，SSH 的 TCP 字节与它口径不同，不能直接相加。
- 新增独立端口统计：SSH 的 TCP 监听端口、HY2 的 UDP 监听端口，均以服务器接收/发送
  的 IP 字节为准；合计分别相加接收、发送，双向总量再相加。
- SSH 统计包含终端、SFTP/SCP、SSH 隧道和重传；不是单独的文件有效载荷。
- 端口被其他程序复用时，也计入该端口统计。不是供应商账单或整机全部流量。
- 使用内核 nftables 命名计数器，不新增常驻进程；手动开启/配置，仅操作
  inet pocket_server_ops_traffic 自有表，不修改既有防火墙表或服务配置。
- 开启时起算，重启、规则被移除或修改端口时重置。相同端口重复开启不重置。
  APP 按服务器缓存最近快照；缓存不能替代服务器持久计数，不提供月度账单历史。
- 刷新状态通过同一次 SSH 请求读取两次计数和系统启动时钟，计算短时速率。
  不引入后台高频轮询；缺失或无权限显示不可用，不当作 0。

## 进度

- [x] 确认原代码尚无 SSH 独立统计，无法直接添加正确合计。
- [x] 统计口径确定，UI 卡片已分配给一个子代理，数据/采集由主代理实现。
- [x] 内核计数器开启、读取、关闭。
- [x] 数据解析、速率、合计、缓存接入。
- [x] 紧凑卡片和配置入口。
- [x] 定向测试及隔离网络命名空间内验证（不触碰宿主机规则）。

参考已读的 Hysteria 官方 Traffic Stats API：
https://v2.hysteria.network/docs/advanced/Traffic-Stats-API/
GET /traffic、GET /online；不调用清零接口。

## 实现与使用

- 仪表盘新增 HY2 + SSH 流量卡，合计置顶，下面是 SSH/TCP 与 HY2/UDP，
  各显示接收、发送、双向累计和刷新采样速率。
- 点击“配置端口”，确认服务器本机监听端口后安装计数规则。支持逗号分隔多个端口。
  SSH 建议值来自 SSH_CONNECTION 中服务器端口，不把 NAT 外部连接端口当作本机端口。
  HY2 建议值来自 ss 的本地 UDP 地址字段；检测不到可手动填写。
- 普通刷新只读规则，不安装软件、改变端口、清零计数或增加常驻服务。
  需要服务器已安装 nftables 且 SSH 用户有操作权限；不支持时显示原因。
- 保留独立 HY2 业务 API 指标，不与端口字节重复叠加。整机网卡指标也不加入两者合计。
- Linux 本机 input/output 端口统计，不覆盖 Docker bridge 转发链里的容器服务；
  容器使用 host 网络的监听端口可使用此统计。端口跳跃需填写实际本机端口。
- UI 复用原仪表盘的每服务器快照缓存；未获取、权限错误、损坏或缺失计数器不补零。
  采样期间规则重建/重置或时间无效时，保留当次可用累计值，不计算虚假速率。

## 验证记录

- 7 项针对本次改动的测试通过：3 项计数/速率/缓存测试、1 项端口输入测试、
  1 项真实 nftables 隔离网络集成测试、1 项控制器仪表盘集成测试、
  1 项 360px 手机宽度卡片和配置弹窗交互测试。
- 真实测试使用 unshare 网络命名空间与一对 veth，TCP/UDP 同用 24443 端口：
  两者分开计数，UDP 四字节有效载荷对应每方向 32 个 IPv4+UDP 字节，合计可核验。
  重复相同配置不清零，修改端口重建统计，关闭只删除自有表，保留原表；
  非 APP 所有的同名表不被覆盖或删除。
- 界面测试发现配置弹窗关闭时 TextEditingController 被提前释放，已经改由
  TextFormField 自主管理控制器，重跑相同交互测试通过。
- 11 个变更代码/测试文件静态分析通过。没有新增依赖，未运行 APK 构建、预览、
  提交或发布，也没有连接/修改用户的真实流量服务器。
- 可复现内核集成测试：设置 PSO_TRAFFIC_NETNS_TEST=1，执行
  flutter test --no-pub --concurrency=1 test/server_traffic_script_test.dart；
  环境需具备 Linux 网络命名空间权限和 nft、ip、nsenter、python3。

## 2026-09-13 beta 发布批次

- 版本 1.0.7-beta.1，Android versionCode 60，发布分支 beta。
- release APK 构建成功（229.8 秒），大小 80,743,681 字节；包名与签名证书
  均与正式版 1.0.6 相同，可覆盖安装。
- 构建复用数据盘 SDK/Gradle 缓存和 build 链接；单 worker，Gradle 堆上限
  1536MiB，构建进程退出后可用内存恢复至约 6.8GB。
- APK：/www/mobile-agent-build/app/outputs/flutter-apk/pocket-server-ops-ai-v1.0.7-beta.1-release.apk。
- SHA-256：b9932bc4f60014b3fca20dbf16d0a871489af8b05efccd0692fada1bf53e9d8a。
- 同步应用内更新清单，并补回已发布正式版 1.0.6 条目。

## 2026-09-13：真实服务器“精品线路c”只读诊断

- 持久 SSH 插件修复后已成功连接，执行用户为 root，nftables 版本为 1.0.6。
- 自有统计表和四个命名计数器都存在且有累计字节。首次读到 SSH 收/发为
  5740 / 7280 字节，HY2 收/发为 110834715 / 43292703 字节。
- 确认 APP 误报根因：该服务器的 nft -j 输出中 table 对象只有 family、name、handle，
  没有 comment；同一张表的 nft -s 文本输出却保留完整 pso-traffic-v1 标记。
  当前 _CounterSample.read 必须从 JSON table.comment 匹配端口和代次，缺字段便返回 null，
  上层因此显示“端口计数器数据无效”，并把端口显示为“未配置”。并非计数器无效或没有权限。
- 先前真实内核测试环境为 nftables 1.0.9，未覆盖这台 1.0.6 的输出差异。
  下一次修复必须增加此真实输出形态的回归，不可仅在原测试环境重跑后宣称解决。
- HY2 运行进程明确使用 /etc/hysteria/config.yaml，服务为 hysteria.service。
  配置监听 :443，没有 trafficStats 键，进程也没有 TCP 统计监听，因此上方
  “HY2 流量统计接口未接入”是独立且准确的提示。端口计数不依赖该 API。
- SSH 实际监听 TCP 22、2222，当前规则仅配置 2222，会漏掉 22 的流量。
- HY2 当前配置和实时监听均为 UDP 443；规则另外包含 35043、41061、41619、43633、
  48820、55584。这些不是当前配置的服务监听端口。APP 现有建议值直接枚举所有
  hysteria UDP socket，无法保证每个都是服务入口；需避免把临时 UDP socket 当作服务端口。
- 建议最小修复：通过文本接口显式读取配置/代次标记，JSON 继续读取实际计数；
  保留重置检测，使用现有规则和累计值，不通过清空表或升级服务器绕过 APP 问题。
  端口建议应识别实际服务监听配置，并继续允许用户手动调整。
- 本次仅诊断并记录文档；未修改 APP 实现、远端规则或 HY2 配置，未重启/安装服务器软件。

## 2026-09-13：nftables 1.0.6 读取修复已完成，待打包

- 采集脚本在每个 JSON 样本前通过 nft -a -s 读取表 handle 和配置 comment，
  分别输出 traffic_meta_before / traffic_meta_after；字节计数仍来自 JSON 命名计数器。
  不新增依赖、不替换规则、不升级远端 nftables。
- APP 核对文本标记的 handle 与同次 JSON table.handle，一致才使用该配置。
  两个速率样本还同时比较 handle 和配置代次；表在读取间被重建时，不拼接两张表的数据，
  重建前后均有效时保留新累计值但不计算跨代次速率。
- SSH 端口建议改为汇总实际 sshd TCP 监听、排序去重，无可见监听信息时才使用
  SSH_CONNECTION 的本机端口。HY2 建议读取运行中 hysteria server 显式 -c/--config
  指定的顶层 listen 配置（支持 --config=、绝对/相对路径、常见 YAML 引号及行尾注释）；
  无明确可读配置时留空供手动填写，不扫描所有 UDP socket 猜测服务入口。
- 在“精品线路c”运行新探针进行只读验证，建议正确返回 SSH 22,2222 与 HY2 443；
  读取并保留现有表 handle 5 和原代次，未清零。完整原始结果保存为
  test/fixtures/nft-1.0.6-traffic-probe.txt，不含账号凭据/配置密钥。
- 该样本在旧读取方式下复现 unavailable；修复后为 ready，合计 2096683694 字节，
  四项累计与服务器真实输出一致，采样速率可用，仪表盘缓存往返一致。
- 新增回归覆盖：旧 nft JSON 缺少 comment、文本/JSON 表身份不一致、跨样本重建，
  以及临时 UDP socket 不进入端口建议。两份定向测试共 9 项通过（含真实隔离内核测试），
  四个变更代码/测试文件静态检查通过。
- 远程插件前两次普通输出没有完整返回 JSON 且 exitCode 缺失；本次只读诊断将相同输出
  gzip+base64 传回后完整取得结果并本地解码验证。没有因此修改 APP SSH 传输策略，
  不据此把传输现象归因于 APP 解析 bug。
- 服务器原规则仍为 SSH 2222 与原 HY2 七个端口；更正统计范围会重新计数，未自动修改。
  HY2 trafficStats 仍未开启，这与端口计数解析修复无关；未改远端配置或重启服务。
- 修复在手机携带的探针/解析逻辑内，需新 APK 生效，无需重装服务器统计规则。
  本批次尚未构建、推送或发布。

## 2026-09-13：beta.2 打包记录

- 用户要求更新试用，版本递增为 1.0.7-beta.2（versionCode 61），同步 beta 更新清单。
- release APK 构建成功，用时 212 秒，大小 80,809,217 字节。
- 包名与原版本一致，签名 SHA-256 仍为
  757ed46c6382c776aa238592fb386587f90f8bba70eaac1f693c3fa237504d19，可覆盖安装。
- 产物：/www/mobile-agent-build/app/outputs/flutter-apk/pocket-server-ops-ai-v1.0.7-beta.2-release.apk。
- 文件 SHA-256：11fde959465d74bee67c32aa97b6578f5ed46977c944ba1bef8e000a81b72f14。
- 使用数据盘缓存、单 worker、1536MiB Gradle 堆；不重跑已通过的功能测试、不启动第二份构建。
